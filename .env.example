# Server configuration
PORT=8000
DB_PATH=./sh.db
HOSTNAME=sh.huny.dev

# Admin authentication token (required for production)
# Generate with: openssl rand -hex 32
ADMIN_TOKEN=your-secret-admin-token-here
