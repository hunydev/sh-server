# Server configuration
PORT=8000
DB_PATH=./sh.db
# HOSTNAME is used for generating curl commands (e.g., sh.example.com)
# If not set, defaults to localhost:8000
HOSTNAME=

# Admin authentication token (optional)
# If not set, admin UI is accessible without authentication
# Generate with: openssl rand -hex 32
ADMIN_TOKEN=
